/// Named-route identifiers used across the app.
///
/// Centralising route names here prevents typos and makes it obvious
/// what destinations exist when wiring `Navigator.pushNamed` calls.
class AppRoutes {
  AppRoutes._();

  static const String root = '/';
}
